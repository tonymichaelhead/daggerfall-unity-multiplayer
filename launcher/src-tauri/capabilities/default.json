{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the DFMP launcher main window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
